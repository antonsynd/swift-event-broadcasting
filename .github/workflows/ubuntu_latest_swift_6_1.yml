name: Ubuntu (latest), Swift 6.1

on:
  push:
    branches:
      - mainline

jobs:
  build:
    name: Ubuntu (latest), Swift 6.1
    runs-on: ubuntu-latest
    steps:
      - uses: swift-actions/setup-swift@v2
        with:
          swift-version: "6.1"
      - uses: actions/checkout@v4
      - name: Build
        run: swift build
      - name: Run tests
        run: swift test
