name: macOS (latest), Swift 6.1

on:
  push:
    branches:
      - mainline

jobs:
  build:
    name: macOS (latest), Swift 6.1
    runs-on: macos-latest
    steps:
      - uses: swift-actions/setup-swift@v2
        with:
          swift-version: "6.1"
      - uses: actions/checkout@v4
      - name: Build
        run: swift build
      - name: Run tests
        run: swift test
