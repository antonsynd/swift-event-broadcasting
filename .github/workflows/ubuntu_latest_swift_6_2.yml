name: Ubuntu (latest), Swift 6.2

on:
  push:
    branches:
      - mainline

jobs:
  build:
    name: Ubuntu (latest), Swift 6.2
    runs-on: ubuntu-latest
    steps:
      - uses: swift-actions/setup-swift@v3
        with:
          swift-version: "6.2"
      - uses: actions/checkout@v5
      - name: Build
        run: swift build
      - name: Run tests
        run: swift test
